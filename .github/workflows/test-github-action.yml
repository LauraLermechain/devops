on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah
      - run: echo "Hello World"

  build2:
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah

  build3:
    runs-on: ubuntu-latest
    needs: 
      - build-2
    steps:
      - run: ls -lah
